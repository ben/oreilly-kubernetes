<li>
    <span class="controls">
        <a href="#" {{action 'delete'}}>❌</a>
        <a href="#" {{action 'toggleComplete'}}>
            {{#if isComplete}}
                🔧
            {{else}}
                ✅
            {{/if}}
        </a>
    </span>
    <a href="#" {{action 'toggleComplete'}} class={{model.state}}>{{model.title}}</a>
</li>
